<div class="row">
  <div class="col-lg-12">
    <div class="page-header">
      <h1>Campeones</h1>
    </div>
    <div class="box-purchase">
      <button type="button" class="btn btn-info btn-block" data-toggle="modal" data-target="#myModalColor">  Agregar Campeon
      </button>
      <%= render :partial => "admin/champions/form"%>
    </div>
  </div>
</div>
<hr>
<table class="table table-striped table-hover table-bordered">
  <thead>
    <tr>
      <th class="center">Nombre</th>
      <th class="center">Raza</th>
      <th class="center">Sexo</th>
      <th class="center">Variedad</th>
      <th class="center">Categoria</th>
      <th class="center">Puesto</th>
      <th class="center">Ganador</th>
      <th class="center">Puntos</th>
      <th class="center">Show</th>
      <th class="center">Fecha</th>
      <th colspan="1"></th>
    </tr>
  </thead>
  <tbody>
     <% @champions.each do |champion| %>
    <tr >
      <td class="center"><%= champion.canine.name %></td>
      <td class="center"><%= get_race(champion.canine.race_id) %></td>
      <td class="center"><%= gender(champion.canine.gender)  %></td>
      <td class="center"><%= get_variety(champion.category_race_variety.variety_id)   %></td>
      <td class="center"><%= get_category(champion.category_race_variety.category_id) %></td>
      <td class="center"><%= champion.position %></td>
      <td class="center"><%= champion.winner %></td>
      <td class="center"><%= champion.points %></td>
      <td class="center"><%= get_show(champion.spectacle_id) %></td>
      <td class="center"><%= champion.date %></td>
      <td class="center"><%= link_to edit_admin_champion_path(champion) do %>
        <i class="fa fa-edit"></i>
        <%end%>
      <%= link_to admin_champion_path(champion), method: :delete, data: { confirm: 'Desea eliminar este champion?' } do %>
        <i class="fa fa-trash-o"></i>
        <%end%>
      </td>
    </tr>
    <% end %>
  </tbody>
</table>

