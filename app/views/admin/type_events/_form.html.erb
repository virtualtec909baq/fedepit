<%= form_for(@type_event) do |f| %>
  <% if @type_event.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@type_event.errors.count, "error") %> prohibited this type_event from being saved:</h2>

      <ul>
      <% @type_event.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
