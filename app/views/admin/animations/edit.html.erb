<div class="row">
  <div class="col-md-12">
    <h2>Editar Video</h2>
    <%= form_for :animation , url: admin_animation_path(@animation), method: :put do |f| %>
      <div id="erros"></div>
      <div class="field">
        <%= f.label :Nombre %><br>
        <%= f.text_field :title, class: 'form-control' %>
      </div>
      <div class="field">
        <%= f.label :url %><br>
        <%= f.text_field :url, class: 'form-control' %>
      </div>
      <div class="field">
        <%= f.label :tipo_video %><br>
        <%= f.collection_select :type_category_id, TypeCategory.all, :id, :description, {prompt: " "}, {class: "form-control"}%>
      </div>
      <div class="field">
        <%= f.label :Descripción %><br>
        <%= f.text_area :description, class: 'form-control' %>
      </div>
      <div class="actions">
       <%= f.submit "ACTUALIZAR VIDEO",  :class  =>"btn btn-primary btn-block" %>
      </div>
    <% end %>
  </div>
</div>
   

