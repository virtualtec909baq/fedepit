<%= form_for(@type_championship) do |f| %>
  <% if @type_championship.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@type_championship.errors.count, "error") %> prohibited this type_championship from being saved:</h2>

      <ul>
      <% @type_championship.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
