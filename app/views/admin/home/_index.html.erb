<script type="text/javascript">
    $(document).ready(function() {
    $("#favoriteFoodTable .deleteLink").on("click",function() {
        var tr = $(this).closest('tr');
        tr.css("background-color","#C9302C");

        tr.fadeOut(400, function(){
            tr.remove();
        });
      return false;
    });
});

</script>
<hr>
 <table class="table table-striped table-hover table-bordered" id="favoriteFoodTable">
    <thead>
      <tr>
        <th class="center"> </th>
        <th class="center"><%= sort_link @search, :name, "Nombre"%></th>
        <th class="center"><%= sort_link @search, :race_id, "Raza"%></th>
        <th class="center">No. Hijos</th>
        <th class="center"> </th>
        <th class="center"> </th>
    </thead>
    <tbody>
      <% @canines.each do |canine| %>
        <tr >
          <td class="center"><a class="deleteLink" href="" >Eliminar</a></td>
          <td class="center"><%= link_to canine.name, admin_canine_path(canine), :class => "link_to"  %></td>
          <td class="center"><%= canine.gender %></td>
          <th class="center"><%=count_children(canine)%></th>
          <%if !enviar_cruce(canine).nil?%>
              <td class="center"> ver mas
                <!-- <ul>
                  <% enviar_cruce(canine).each do |feature| %>
                    <li>
                    <span><%= feature[0]%>: </span>
                    <% average =  (feature[1].to_f/$total_children) * 100 %>
                    <span> <%= number_to_percentage(average, precision: 0) %></span>
                    </li>
                  <%end%>
                 </ul> -->
              </td>
          <%else%>
          <td class="center"></td>
          <%end%>
          <td class="center"> <%= link_to "cruzar", admin_mergecanines_path(canine)%></td>
        </tr>
      <% end %>
    </tbody>
  </table> 


