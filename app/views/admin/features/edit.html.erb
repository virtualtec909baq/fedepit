<div class="row">
  <div class="col-md-12">
    <%= form_for @feature , url: admin_feature_path(@feature), :id => @feature.id , method: :put do |f| %>
    <% if @feature.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@feature.errors.count, "error") %> prohibited this breeder from being saved:</h2>

      <ul>
        <% @feature.errors.full_messages.each do |message| %>
        <li><%= message %></li>
        <% end %>
      </ul>
    </div>
    <% end %>

    <% end %>
  </div>
</div>