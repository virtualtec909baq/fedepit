<!-- Modal -->
<div class="modal fade" id="myModalVideos" tabindex="-1" role="dialog" aria-labelledby="myModalLabel" aria-hidden="true">
  <div class="modal-dialog">
    <div class="modal-content">
      <div class="modal-header">
        <button type="button" class="close" data-dismiss="modal" aria-label="Close"><span aria-hidden="true">&times;</span></button>
        <h4 class="modal-title" id="myModalLabel">Agregar Video</h4>
      </div>
      <div class="modal-body">
        <%= form_for :video , url: admin_videos_path(@video), method: :post do |f| %>
          <div class="field">
            <%= f.label :Nombre %><br>
            <%= f.text_field :title, class: 'form-control' %>
          </div>
          <div class="field">
            <%= f.label :url %><br>
            <%= f.text_field :url, class: 'form-control' %>
          </div>
          <div class="field">
            <%= f.label :tipo_video %><br>
            <%= f.collection_select :type_category, TypeCategory.all, :id, :description, {prompt: " "}, {class: "form-control"}%>
          </div>
          <div class="field">
            <%= f.label :Descripción %><br>
            <%= f.text_area :description, class: 'form-control' %>
          </div>
          <div class="actions">
            <%= f.submit :class =>"btn btn-primary btn-block" %>
          </div>
        <% end %>
      </div>
    </div>
  </div>
</div>


