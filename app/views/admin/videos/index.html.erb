<div class="row">
  <div class="col-lg-12">
    <div class="page-header">
      <h1>Mis Videos</h1>
    </div>
    <div class="box-purchase">
      <%= render :partial => "admin/videos/filters"%>
    </div>
  </div>
</div>
<hr>
<table class="table table-striped table-hover table-bordered">
  <thead>
    <tr>
      <th class="center"><%= sort_link @search, :title, "Title"%></th>
      <th class="center">Url</th>
      <th class="center">Description</th>
      <th colspan="3"></th>
    </tr>
  </thead>
  <tbody>
    <% @videos.each do |video| %>
    <tr>
      <td class="center"><%= link_to video.title, admin_video_path(video), :class => "link_to"  %></td>
      <td ><%= video.url %></td>
      <td ><%= video.description %></td>
      <td class="center"><%= link_to edit_admin_video_path(video) do %>
        <i class="fa fa-edit"></i>
        <%end%>
      </td>
      <td class="center"><%= link_to admin_video_path(video), method: :delete, data: { confirm: 'Are you sure?' } do %>
        <i class="fa fa-trash-o"></i>
        <%end%>
      </td>

    </tr>
    <% end %>
  </tbody>
</table>
<%= will_paginate @videos, :locals => { :videos => @videos }, renderer: BootstrapPagination::Rails%>



