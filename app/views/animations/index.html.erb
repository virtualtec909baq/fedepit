<div class="breadcrumb-color">
  <div class="container">
    <div class="row">
      <div class="col-lg-12">
        <ol class="breadcrumb">
          <li>
            <a href="<%= root_path%>"><%= t('.home')%></a>
          </li>
          <li class="active"><%= t('.video')%></li>
        </ol>
      </div>
    </div>
  </div>
</div>

<div class="container">
    <div class="box-purchase" style="margin-bottom: 20px;">
      <%= render :partial => "/animations/filters"%>
    </div>

    <%- if params[:q]%>
    <%if params[:locale] == "en"%>
      <table class="table table-striped table-hover table-bordered">
        <thead>
          <tr>
            <th class="center">ID</th>
            <th class="center"><%= sort_link @search, :title, "Title"%></th>
            <th class="center"><%= sort_link @search, :desciption, "Desciption"%></th>
          </tr>
        </thead>
        <tbody>
          <% @animations.each do |animation| %>
          <tr >
            <td class="center"><%= animation.id %></td>
            <td class="center"><%= link_to animation.title, animation_path(animation.id), :class => "link_to"  %></td>
            <td class="center"><%= truncate(animation.description, :ommision => "", :length => 40) %></td>
          </tr>
          <% end %>
        </tbody>
      </table>
    <%-else%>
     <table class="table table-striped table-hover table-bordered">
        <thead>
          <tr>
            <th class="center">ID</th>
            <th class="center"><%= sort_link @search, :title, "Titulo"%></th>
            <th class="center"><%= sort_link @search, :desciption, "Descripción"%></th>
          </tr>
        </thead>
        <tbody>
          <% @animations.each do |animation| %>
          <tr >
            <td class="center"><%= animation.id %></td>
            <td class="center"><%= link_to animation.title, animation_path(animation.id), :class => "link_to"  %></td>
            <td class="center"><%= truncate(animation.description, :ommision => "", :length => 40) %></td>
          </tr>
          <% end %>
        </tbody>
      </table>
    <%end%>


    <%= will_paginate @animations, :locals => { :animations => @animations }, renderer: BootstrapPagination::Rails%>
    <% else %>
      <br>
      <h2><%= t('.video')%></h2>
    <%end%>
</div>


